#!/usr/bin/env bash
set -euo pipefail

if [ "${1:-}" = "" ]; then
  echo "usage: pronto-ai-audit-agent <auditor_id> [extra flags]" 1>&2
  exit 2
fi

auditor_id="$1"
shift

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/pronto-ai-audit" --agent "$auditor_id" "$@"
