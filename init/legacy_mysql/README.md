# legacy_mysql

Este folder contiene SQL legacy (principalmente estilo MySQL) movido aqui para cumplir la politica:
- Unica carpeta `migrations/` valida: `pronto-scripts/init/sql/migrations/`

No debe ser ejecutado por `pronto-init` ni `pronto-migrate`.
