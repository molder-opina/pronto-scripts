# Configuración para scripts de negocio PRONTO
# Copiar este archivo a .env y completar los valores

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pronto
DB_USER=pronto
DB_PASSWORD=pronto

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Secret (debe coincidir con la aplicación)
JWT_SECRET=your-jwt-secret-here

# URL de la API
API_URL=http://localhost:6080

# Scope por defecto (para scripts que lo requieran)
DEFAULT_SCOPE=admin
